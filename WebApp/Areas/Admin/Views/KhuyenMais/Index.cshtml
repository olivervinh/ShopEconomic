@model IEnumerable<WebApp.Areas.Admin.Models.KhuyenMai>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Tenkhuyenmai)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TrangThai)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tenkhuyenmai)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TrangThai)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Makhuyenmai">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Makhuyenmai">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Makhuyenmai">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
